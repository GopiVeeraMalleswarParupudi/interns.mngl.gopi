'''
write a program which will accept a word and determine whether the given word is palindrome or not 
by using lambda functions.
'''
palindrome = lambda word : 'palindrome' if word== else 'not a palindrome'

word=input('Enter the word : ')
palindrome(word)